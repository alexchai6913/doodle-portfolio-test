# Doodle Portfolio — Test Page

This repository is just a test page. It is a single-file HTML portfolio mock-up
("Creative Doodler") built with Bootstrap 4 CSS only — no custom stylesheets and
no script tags. Inline `onclick` handlers provide smooth anchor scrolling.

Not for production use.
